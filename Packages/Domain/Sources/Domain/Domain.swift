// ── FILE: Packages/Domain/Sources/Domain/Domain.swift ──
//
// Domain module — pure business types: entities, repository protocols, use
// cases. Depends only on Core; never imports SwiftUI, SwiftData, or UIKit.
//
// PR 1 placeholder. Real types land in PR 2.

import Foundation
import Core

public enum DomainModule {
    public static let identifier = "com.SamuraiStudios.Castle.Domain"
}
